# Read input
n = input().strip()

# Get first and last digits
first_digit = int(n[0])
last_digit = int(n[-1])

# Calculate sum
result = first_digit + last_digit

# Print result
print(result)